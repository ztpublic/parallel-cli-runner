# AGENTS.md

- Frontend is React + Vite + TypeScript.
- Entry points: `src/main.tsx` and `src/App.tsx`.
- UI building blocks live in `src/components`, hooks in `src/hooks`, services in `src/services`, and types in `src/types`.
- When UI behavior changes, update or add stories under `src/stories` if applicable.
